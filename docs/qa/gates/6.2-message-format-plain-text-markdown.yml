schema: 1
story: "6.2"
story_title: "메시지 포맷(Plain Text / Markdown 겸용)"
gate: "PASS"
status_reason: "PRD 9.2 템플릿(A/B/C) 형태로 Telegram 알림 문자열을 만들고, 포맷터 내부에서 user_id/question 마스킹을 다시 적용합니다. `python3 -m pytest -q tests/unit/test_telegram_message.py` 39개 테스트가 통과했습니다."
reviewer: "Quinn (Test Architect)"
updated: "2025-12-25T09:20:54Z"

waiver: { active: false }

top_issues:
  - id: "REL-001"
    severity: low
    finding: "Telegram `parse_mode=Markdown`라서, 질문/초안에 특수문자(`_`, `*` 등)가 많으면 메시지 모양이 깨질 수 있습니다."
    suggested_action: "선택: 텍스트를 Markdown escape 하거나, parse_mode를 `HTML`로 바꾸고 HTML escape 규칙을 정하세요."
    suggested_owner: "dev"
  - id: "OPS-001"
    severity: low
    finding: "현재 예시 worker 흐름에서는 Sheets 실패(E005)만 템플릿 B(시스템 오류)로 보냅니다."
    suggested_action: "선택: error_code/stage가 있을 때 템플릿 B를 쓰는 공통 분기(매핑)를 추가하세요."
    suggested_owner: "dev"

risk_summary:
  totals: { critical: 0, high: 0, medium: 1, low: 2 }
  recommendations:
    must_fix: []
    monitor:
      - "Telegram/로그에 원문(question_raw) 출력 금지(마스킹 유지)"
      - "Markdown 특수문자 케이스에서 Telegram 전송/표시가 깨지지 않는지 운영에서 1회 확인"

quality_score: 100
expires: "2026-01-08T09:20:54Z"

evidence:
  tests_reviewed: 39
  risks_identified: 3
  trace:
    ac_covered: [1, 2, 3, 4, 5]
    ac_gaps: []

nfr_validation:
  security:
    status: PASS
    notes: "템플릿 A/B/C 포맷터에서 `mask_user_id()`/`mask_question()`을 다시 적용합니다(최종 방어선)."
  performance:
    status: PASS
    notes: "문자열 조합/마스킹 + Telegram 전송 1회 수준이라 비용이 작습니다."
  reliability:
    status: PASS
    notes: "포맷터는 순수 함수이고, `python3 -m pytest -q tests/unit/test_telegram_message.py`가 통과합니다."
  maintainability:
    status: PASS
    notes: "템플릿이 A/B/C로 분리되어 있고, 유닛 테스트가 각 템플릿을 따로 보호합니다."

recommendations:
  immediate: []
  future:
    - action: "Telegram 메시지 Markdown escape/규칙(또는 HTML 전환) 정하기"
      refs:
        ["src/layers/shared/python/talktalk_shared/clients/telegram_client.py", "src/functions/worker/telegram_message.py"]
    - action: "시스템 오류 템플릿(B) 적용 범위 확장(필요 시)"
      refs: ["src/functions/worker/pipeline.py"]

