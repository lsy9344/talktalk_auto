schema: 1
story: "5.3"
story_title: "운영 규칙"
gate: "PASS"
status_reason: "TEST/PROD 운영 규칙(`action_taken`)과 PII 마스킹이 유닛 테스트로 확인되며, pytest/ruff/mypy(src) 재검증도 통과합니다."
reviewer: "Quinn (Test Architect)"
updated: "2025-12-27T17:42:02Z"

waiver: { active: false }

top_issues:
  - id: "LOG-001"
    severity: low
    finding: "`pipeline.py`에서 `telegram_alert_sent` 값이 SheetsLogRow에 반영되지 않아(기본값 false로 저장), 운영자가 시트에서 알림 전송 여부를 오해할 수 있습니다."
    suggested_action: "선택: Telegram 전송 시도 후 `SheetsLogRow`를 만들거나, `telegram_alert_sent`가 의미하는 바(전송 성공 vs 알림 트리거)를 문서/코드로 고정하세요."
  - id: "SEND-001"
    severity: low
    finding: "PROD에서 실제 발송 결과(`send_success`, `talktalk_send_result`)를 `pipeline.py`가 아직 채우지 않습니다(현재는 placeholder)."
    suggested_action: "Story 2.4의 `send_answer_if_allowed()`를 Worker 흐름에 연결하고, 그 결과로 `action_taken`/`talktalk_send_result`를 채우는 end-to-end 테스트를 추가하세요."

risk_summary:
  totals: { critical: 0, high: 0, medium: 1, low: 1 }
  recommendations:
    must_fix: []
    monitor:
      - "운영 로그/알림에서 PII가 새지 않도록 마스킹 규칙 유지"
      - "`telegram_alert_sent` 필드 의미(전송 성공/트리거 여부)를 팀에서 합의"

quality_score: 100
expires: "2026-01-10T17:42:02Z"

evidence:
  tests_reviewed: 390
  risks_identified: 2
  trace:
    ac_covered: [1, 2, 3, 4]
    ac_gaps: []

nfr_validation:
  security:
    status: PASS
    notes: "CloudWatch/Telegram 출력은 `mask_question()`/`mask_user_id()`를 사용하고, 시트에는 원문/마스킹을 분리 저장합니다."
  performance:
    status: PASS
    notes: "문자열 처리(줄바꿈/500자/마스킹) + Sheets append로 비용이 작습니다."
  reliability:
    status: PASS
    notes: "RAG chunk 타입 불일치로 인한 런타임 에러 가능성을 제거했고, pytest가 통과합니다."
  maintainability:
    status: PASS
    notes: "운영 규칙/로그 row builder를 모듈로 분리했고, ruff/mypy로 품질을 유지합니다."

recommendations:
  immediate: []
  future:
    - action: "`telegram_alert_sent` 값을 실제 전송 결과로 시트에 남길지 정책 확정"
      refs: ["src/functions/worker/pipeline.py", "src/functions/worker/sheets_logging.py"]
    - action: "PROD 발송 결과를 `pipeline.py`에 연결하고 end-to-end 테스트 추가"
      refs: ["src/functions/worker/pipeline.py", "src/functions/worker/send_answer.py", "src/functions/worker/operational_rules.py"]
