# Risk Profile - Story 7.1 (2025-12-25)

## 전체 판단

- **리스크 수준**: 낮음(Low)
- 이유: 이번 Story는 “새 기능 추가”보다 “기존 규칙을 테스트로 고정”하는 작업이 중심입니다. 전체 테스트 379개가 통과했습니다.

## 주요 리스크와 확인 결과

1. **SEC-001: TEST 모드에서 실수로 자동 발송이 나가는 위험**
   - 확인: `make_send_decision()`의 1~2번 게이트(global/channel)가 TEST면 무조건 발송을 막습니다.
   - 테스트: `tests/unit/test_send_answer_test_mode.py`에서 TEST 모드일 때 TalkTalk Send API 호출이 0번임을 확인합니다.

2. **REL-001: Sheets 기록 실패로 운영 로그가 빠지는 위험**
   - 확인: Worker 파이프라인은 `append_log_row()`를 항상 시도하고, 실패(E005)는 이유에 남긴 뒤 계속 진행합니다.
   - 테스트: `tests/unit/test_pipeline_sheets_logging.py`에서 성공/실패(예외, Circuit Breaker) 모두 확인합니다.

3. **OPS-001: 웹훅 ACK가 느려져서 타임아웃/재시도가 생기는 위험**
   - 확인: Ingest는 검증 + dedup + SQS enqueue만 하고 200 OK를 반환합니다. (OpenAI/Sheets/Telegram 같은 무거운 작업 없음)
   - 테스트: `tests/unit/test_ingest_quick_ack.py`에서 200 OK 반환, SQS enqueue, 그리고 무거운 작업이 호출되지 않음을 확인합니다.
   - 제안: 운영에서는 실제 응답 시간(Lambda duration)을 CloudWatch로 측정해서 3s/5s 한도 안에 들어오는지 감시하세요.

