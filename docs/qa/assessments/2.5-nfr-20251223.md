# NFR Assessment - Story 2.5 (2025-12-23)

## Security (보안)

- 상태: PASS
- 확인:
  - user_id/user_key/질문 원문이 로그에 남지 않도록 마스킹을 적용했습니다.
  - 집계 완료 로그에도 `question_masked`만 남기도록 정리했습니다.

## Performance (성능)

- 상태: PASS
- 확인:
  - “즉시 처리” 대신 Delay Queue(30초)로 집계하여 LLM 호출 수를 줄이는 구조입니다.
  - 최대 메시지 수(기본 10)와 최종 500자 제한으로 비용 폭증을 막습니다.

## Reliability (신뢰성)

- 상태: PASS
- 확인:
  - 중복/늦은 트리거는 status 체크로 무시합니다.
  - 만료 후 들어온 메시지는 새 집계 세션을 시작합니다(AC7).

## Maintainability (유지보수)

- 상태: PASS
- 확인:
  - `pipeline.py`로 진입점을 분리해 순환 import를 피했습니다.
  - pytest/ruff/mypy(src) 통과 + 유닛 테스트로 핵심 집계 로직을 보호합니다.
