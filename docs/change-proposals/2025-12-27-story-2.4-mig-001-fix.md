# Sprint Change Proposal: Story 2.4 MIG-001 Fix

## Metadata

- **Date**: 2025-12-27
- **Story**: 2.4 - PROD 전송(보내기 API) 구현 (TalkTalk Send API Integration)
- **Issue ID**: MIG-001
- **Severity**: Medium
- **Author**: Bob (Scrum Master)
- **Reviewed By**: N/A (Pending QA re-run)

## Change Context

### Original Issue

QA Gate `docs/qa/gates/2.4-talktalk-send-api-integration.yml`에서 **CONCERNS** 상태로 마킹됨.

**MIG-001 Finding:**
> 스토리(AC/Tasks/Dev Notes)에는 `talktalk_auth_secret_arn` fallback 문구가 남아 있는데, 현재 코드는 SSM(`talktalk_auth_parameter_name`)만 사용합니다.

### Root Cause

- **2025-12-27**: James (Developer)가 코드를 AWS Secrets Manager → SSM Parameter Store로 마이그레이션 완료
- **문서**: Story 2.4의 AC/Tasks/Dev Notes에 "(마이그레이션 기간: 새 필드가 없으면 `talktalk_auth_secret_arn`도 임시로 허용)" 문구가 그대로 남아있음
- **결과**: 문서와 구현이 불일치

### Impact Analysis

#### Epic/Story Impact
- ✅ **기능**: 정상 작동 (pytest 390, ruff, mypy 통과)
- ⚠️ **문서**: AC 4, Task 2, Task 3, Dev Notes에 fallback 문구 존재
- ⚠️ **QA Gate**: CONCERNS → 문서 정리 후 PASS 가능

#### Artifact Conflict
- **None**: 코드는 이미 SSM-only로 완료
- **Documentation only**: Story 파일만 수정 필요

#### Path Forward
✅ **Path A (Selected)**: 문서에서 fallback 문구 제거 (마이그레이션 완료 반영)
❌ **Path B (Rejected)**: 코드를 fallback 지원으로 되돌리기 (비효율적, 이미 마이그레이션 완료)

## Proposed Changes

### Artifact: `docs/stories/2.4.story.md`

#### Change 1: Acceptance Criterion 4 (Line 23)

**From:**
```markdown
4. Authorization 토큰은 하드코딩하지 않는다. 채널별 토큰을 사용할 수 있게 **ChannelConfig의 `talktalk_auth_parameter_name`**(SSM Parameter 이름/경로)을 저장하고, 실행 중에 SSM Parameter Store(SecureString)에서 읽어온다. (마이그레이션 기간: 새 필드가 없으면 `talktalk_auth_secret_arn`도 임시로 허용) (로컬/테스트에서는 토큰을 함수 인자로 주입 가능)
```

**To:**
```markdown
4. Authorization 토큰은 하드코딩하지 않는다. 채널별 토큰을 사용할 수 있게 **ChannelConfig의 `talktalk_auth_parameter_name`**(SSM Parameter 이름/경로)을 저장하고, 실행 중에 SSM Parameter Store(SecureString)에서 읽어온다. (로컬/테스트에서는 토큰을 함수 인자로 주입 가능)
```

#### Change 2: Task 2 Description (Lines 61-64)

**From:**
```markdown
- [x] **Task 2: 채널 토큰 가져오기(SSM Parameter Store) 유틸 추가** (AC: 4, 6)
  - [x] `src/layers/shared/python/talktalk_shared/utils/secrets.py`를 만든다. (SSM Parameter Store 조회 + 간단 캐시)
  - [x] 입력: `secret_arn: str` → 출력: `secret_string: str`
  - [x] 실패 시에는 예외를 올리되, 상위에서 `E006`으로 처리할 수 있게 에러 타입/메시지는 너무 길지 않게 한다(토큰 값 로그 금지).
```

**To:**
```markdown
- [x] **Task 2: 채널 토큰 가져오기(SSM Parameter Store) 유틸 추가** (AC: 4, 6)
  - [x] `src/layers/shared/python/talktalk_shared/utils/secrets.py`를 만든다. (SSM Parameter Store 조회 + 간단 캐시)
  - [x] 입력: `parameter_name: str` → 출력: `parameter_value: str`
  - [x] 실패 시에는 예외를 올리되, 상위에서 `E006`으로 처리할 수 있게 에러 타입/메시지는 너무 길지 않게 한다(토큰 값 로그 금지).
```

#### Change 3: Task 3 Flow (Line 71)

**From:**
```markdown
    - [x] (운영) 옵션 인자가 없으면 `talktalk_auth_parameter_name`으로 SSM Parameter Store에서 토큰을 읽는다. (마이그레이션 기간: 새 필드가 없으면 `talktalk_auth_secret_arn`도 fallback으로 허용)
```

**To:**
```markdown
    - [x] (운영) 옵션 인자가 없으면 `talktalk_auth_parameter_name`으로 SSM Parameter Store에서 토큰을 읽는다.
```

#### Change 4: Dev Notes - Dependencies (Line 95)

**From:**
```markdown
- ChannelConfig에 `talktalk_auth_parameter_name` 값(파라미터 이름/경로)이 실제로 들어 있어야 합니다. (마이그레이션 기간에는 `talktalk_auth_secret_arn`도 임시로 허용)
```

**To:**
```markdown
- ChannelConfig에 `talktalk_auth_parameter_name` 값(파라미터 이름/경로)이 실제로 들어 있어야 합니다.
```

#### Change 5: Dev Notes - 배포/권한 메모 (Line 99)

**From:**
```markdown
- AWS에서 SSM Parameter Store를 읽으려면 Lambda 실행 역할에 `ssm:GetParameter` 권한이 필요합니다. (마이그레이션 기간에만 `secretsmanager:GetSecretValue`도 필요할 수 있음)
```

**To:**
```markdown
- AWS에서 SSM Parameter Store를 읽으려면 Lambda 실행 역할에 `ssm:GetParameter` 권한이 필요합니다.
```

#### Change 6: Dev Notes - 토큰/시크릿 (Line 122)

**From:**
```markdown
- 채널 설정(ChannelConfig)에 `talktalk_auth_parameter_name`이 있습니다.
- 이 이름(경로)으로 SSM Parameter Store에서 채널별 Authorization 토큰을 읽습니다. (마이그레이션 기간: 새 필드가 없으면 `talktalk_auth_secret_arn` fallback)
- 토큰 값은 로그/에러/Telegram에 절대 남기지 않습니다.
```

**To:**
```markdown
- 채널 설정(ChannelConfig)에 `talktalk_auth_parameter_name`이 있습니다.
- 이 이름(경로)으로 SSM Parameter Store에서 채널별 Authorization 토큰을 읽습니다.
- 토큰 값은 로그/에러/Telegram에 절대 남기지 않습니다.
```

#### Change 7: Change Log (New Entry)

**Add:**
```markdown
| 2025-12-27 | 1.8 | MIG-001 Fixed: Remove all talktalk_auth_secret_arn fallback references from AC/Tasks/Dev Notes | Bob (Scrum Master) |
```

#### Change 8: Status Update

**From:**
```markdown
Ready for Review (QA re-run requested: MIG-001 documented, Story Owner approval needed for doc updates)
```

**To:**
```markdown
Ready for Review (MIG-001 fixed: Documentation updated to SSM-only, QA re-run requested)
```

## Analysis Summary

### Rationale

1. **Code is SSM-only**: 2025-12-27에 James가 마이그레이션 완료
2. **Documentation lag**: Story 문서에만 fallback 문구가 남아있음
3. **No code changes needed**: 구현은 이미 올바름
4. **Documentation update only**: AC/Tasks/Dev Notes 정리만 필요

### Verification

- ✅ pytest 390 통과
- ✅ ruff 통과
- ✅ mypy src 통과
- ✅ 코드는 `talktalk_auth_parameter_name`만 사용

### Expected Outcome

- QA Gate `CONCERNS` → `PASS`
- 문서와 구현 일치
- Story 2.4 완료 가능

## Next Steps

1. ✅ **Completed**: Story 2.4 문서 수정 완료 (this proposal)
2. ⏳ **Pending**: QA re-run `/review-story 2.4` 실행 요청
3. ⏳ **Expected**: Gate `PASS` → Story Status `Ready for Done`

## Approval Status

- **Proposal Created**: 2025-12-27
- **Implemented**: Yes (document updates applied)
- **QA Re-run Required**: Yes
- **Final Approval**: Pending (Story Owner / QA)

---

**Generated by**: Bob (Scrum Master)
**Tool**: Correct Course Task (YOLO Mode - direct fix applied)
